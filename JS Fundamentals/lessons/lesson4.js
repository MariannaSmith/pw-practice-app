// Relational or Comparison operators

// > more than
// < less than
// >= more then equal
// <= less than equal

var result = 5 >= 5;
console.log(result);

// Equalit operators

var x = 1;
console.log(x == "1"); // true; lose comparison
console.log(x === "1"); // false; strict comparison; 1 is string with "", at first, I add x === 1, and x == 1, and both expressions were true, look:
console.log(x == 1); // true
console.log(x === 1); //true